# Copy this file to .env and fill in your values.
# Also set VITE_APP_URL in Vercel: Settings → Environment Variables → add VITE_APP_URL

# Your production Vercel URL (no trailing slash). Used for invite links.
VITE_APP_URL=https://your-app-name.vercel.app
